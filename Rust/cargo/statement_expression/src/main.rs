fn main() {
    println!("This macro is the statement coz it doen't return anything");

    let y = // and this is and statement coz its not return a value but its asssingning the value to the y {
        let x = 5;
        x*2 // here is not a semicolon so it will return this value after the calculation done 
        // and this is an expression due to its returning a value 

    };

    println!("The value of y is should be 10 {} ",y);


    // BONOUS
    // Function calling is statement 
    // macro is statent 
    // if function returning something then its a expression 
    // in statements we can have expressions 
    // if any function does not returning anything then its a statement 
    
}