#include<iostream>
#include<string>
#include<algorithm>
using namespace std;
int main () {
    // substr(); function 
    string str = "Gaurav";
        // stringName.substr( startingIndex , length) // length optional. 
        // and starting index is also optional 
        cout << str.substr(1,4);
        

}