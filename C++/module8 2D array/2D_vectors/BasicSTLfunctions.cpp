#include<iostream>
#include<vector>
using namespace std;
int main() {
    vector<vector<int>> vec (2, vector<int> (2, 11));

    /*
    1) push_back() 
    2) pop_back()
    3) begin()
    4) end()
    5) rbegin()
    6) rend()
    */
    
    //  when we use push_back in 2D vector an entire vector we have to insert into that for ex. : entire coloumn 
    // similarly when we use pop_back entire last coloumn or vector will be removed from the vector 
    
}